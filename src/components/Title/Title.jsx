import React from "react";

export const Title = ({ title }) => {
  return (
    <h2 className=" mb-6 text-white text-[44px] font-black font-grotesk uppercase text-center leading-10">
      {title}
    </h2>
  );
};
